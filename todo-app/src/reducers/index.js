

const initialState = {
    todos: [{
        item: 'Learn about reducers',
        completed: false,
        id:
    }]
}

export const reducer = (state, action) => {
    switch (action.type) {

        default:
            return state
    }
}